Downloads

  Binaries, source, and JavaDoc are available through Maven Central 
  under the group ID <<${project.groupId}>> and the artifact ID 
  <<${project.artifactId}>>.
  
+------------------------------------------------------
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>${project.artifactId}</artifactId>
            <version>${project.version}</version>
            <type>${project.packaging}</type>
        </dependency>
+------------------------------------------------------